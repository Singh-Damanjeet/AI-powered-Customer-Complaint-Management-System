function App() {
  return (
    <main className="foundation-shell">
      <section className="foundation-card" aria-labelledby="app-title">
        <span className="eyebrow">PHARMA COMPLAINT AI</span>
        <h1 id="app-title">Foundation ready</h1>
        <p>
          The React application is connected to its local foundation. Complaint
          workflows will be introduced in a later phase.
        </p>
      </section>
    </main>
  )
}

export default App
